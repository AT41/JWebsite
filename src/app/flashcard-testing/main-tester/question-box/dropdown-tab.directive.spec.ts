import { DropdownTabDirective } from './dropdown-tab.directive';

describe('DropdownTabDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownTabDirective();
    expect(directive).toBeTruthy();
  });
});
